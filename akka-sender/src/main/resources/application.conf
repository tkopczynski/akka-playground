akka {
  loglevel = "INFO"

  actor {
    provider = remote

    remote {
      enabled-transports = ["akka.remote.netty.tcp"]
      netty.tcp {
        hostname = "akka-sender"
        port = 2552
      }
    }
  }
}